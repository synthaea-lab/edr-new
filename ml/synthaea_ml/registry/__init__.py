"""Registry-side helpers: model card training records, dataset version bindings.

See `synthaea_ml.registry.training_record` for the machine-parsable record that
sits next to a model card (`card.md`) and locks the dataset versions it was
trained on (rule 3 of ml/README.md).
"""
