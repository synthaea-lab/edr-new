# CI Workflows

Planned pipelines:
- `ci.yml` — fmt, clippy, tests on the Rust workspace (Linux + Windows + macOS runners)
- `ml.yml` — ruff + pytest on the Python pipeline, ONNX parity check
- `content.yml` — Sigma rule linting and validation
