//! # cli
//!
//! The admin command-line tool for the endpoint: status, sensor health, recent
//! detections, policy version, and policy-gated actions. A pure client of the agent
//! over `ipc`, exactly like the endpoint UI — it holds no privileges of its own and
//! works on headless hosts where no UI ships.

fn main() {
    // Intentionally empty — skeleton only.
}
