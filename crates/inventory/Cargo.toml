[package]
name = "inventory"
version = "0.1.0"
description = "Software and asset inventory: packages, services, autoruns, listening ports"
license.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
