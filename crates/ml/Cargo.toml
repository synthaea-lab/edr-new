[package]
name = "ml"
version = "0.1.0"
description = "On-device ML inference (ONNX) and feature extraction"
license.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
schema = { path = "../schema" }
correlator = { path = "../correlator" }
thiserror = { workspace = true }
ort = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
# Integration tests build event windows to score end-to-end (events -> EventBus ->
# vector -> model); the unit path already depends on both.
schema = { path = "../schema" }
correlator = { path = "../correlator" }
