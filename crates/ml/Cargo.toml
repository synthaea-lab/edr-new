[package]
name = "ml"
version = "0.1.0"
description = "On-device ML inference (ONNX) and feature extraction"
license.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
schema = { path = "../schema" }
correlator = { path = "../correlator" }
thiserror = { workspace = true }
ort = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
# capture_parity: deserialize a real events.jsonl as schema::Event and run it
# through the correlator's window — the crate already depends on both.
schema = { path = "../schema" }
correlator = { path = "../correlator" }
