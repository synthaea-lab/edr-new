[package]
name = "enrich"
version = "0.1.0"
description = "Cross-platform enrichment: SHA-256 hashing (cached), code-signature verification"
license.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
schema = { path = "../schema" }
store = { path = "../store" }
sha2 = "0.10"
log = { version = "0.4", default-features = false }
