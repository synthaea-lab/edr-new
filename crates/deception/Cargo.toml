[package]
name = "deception"
version = "0.1.0"
description = "Deception: per-host canary files, credentials, and tripwires with near-zero-FP detections"
license.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
