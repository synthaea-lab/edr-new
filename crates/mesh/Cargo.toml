[package]
name = "mesh"
version = "0.1.0"
description = "Agent peer mesh: mutual attestation and posture gossip between nearby agents"
license.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
