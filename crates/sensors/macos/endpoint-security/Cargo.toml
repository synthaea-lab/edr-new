[package]
name = "sensor-macos"
version = "0.1.0"
description = "macOS sensor: EndpointSecurity-based telemetry"
license.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
