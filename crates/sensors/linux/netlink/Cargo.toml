[package]
name = "sensor-linux-netlink"
version = "0.1.0"
description = "Linux netlink telemetry: socket tables, conntrack, proc connector"
license.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
schema = { path = "../../../schema" }

[target.'cfg(target_os = "linux")'.dependencies]
libc = { version = "0.2", default-features = false }
