[package]
name = "store"
version = "0.1.0"
description = "Bounded local state: LRU-bounded maps for detection state, durable event spool"
license.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
log = { version = "0.4", default-features = false }

[dev-dependencies]
schema = { path = "../schema" }
