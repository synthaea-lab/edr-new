[package]
name = "schema"
version = "0.1.0"
description = "Shared event types and the Sensor/EventSink contract"
license.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
