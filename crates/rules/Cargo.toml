[package]
name = "rules"
version = "0.1.0"
description = "Rule engine: stateless and stateful detections"
license.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
policy = { path = "../policy" }
schema = { path = "../schema" }
store = { path = "../store" }

[dev-dependencies]
schema = { path = "../schema", features = ["test-fixtures"] }
