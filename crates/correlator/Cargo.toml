[package]
name = "correlator"
version = "0.1.0"
description = "Temporal correlation and scoring: co-occurrence rules and Bayesian belief per entity"
license.workspace = true
edition.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
policy = { path = "../policy" }
schema = { path = "../schema" }
store = { path = "../store" }

[dev-dependencies]
schema = { path = "../schema", features = ["test-fixtures"] }
