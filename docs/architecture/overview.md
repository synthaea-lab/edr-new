# Architecture Overview

The system at a glance: endpoint agent pipeline (sensor -> normalizer -> detection ->
correlation -> verdict -> response) and the control plane it reports to. Entry point for
all other architecture documents.
