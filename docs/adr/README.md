# Architecture Decision Records

Locked decisions and their consequences. Numbered (`NNNN-short-title.md`), immutable once
accepted, superseded rather than edited. Decisions from the old iteration only enter by
being explicitly re-adopted. Use `template.md` for new ADRs.
