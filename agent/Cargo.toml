[package]
name = "agent"
version = "0.1.0"
description = "The Synthaea agent binary — wires sensors, detection, and response together"
license.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
