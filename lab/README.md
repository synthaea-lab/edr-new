# lab — Test Lab

Reproducible environment for validating detections end to end.

| Path | Purpose |
| --- | --- |
| `scenarios/` | Scripted attack scenarios (beacon, dropper chain, respawning beacon, ...) run against a live agent |
| `vagrant/` (planned) | VM definitions and provisioning for Linux/Windows test machines |

To be migrated from `old/lab`. Scenarios are for detection validation in the lab only.
