# Previous project iteration — reference only, never tracked here
/old/

# Rust
/target/
**/target/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# ML artifacts (large binaries are versioned elsewhere); the one exception is the
# small committed parity fixture consumed by cargo test in CI.
*.onnx
!crates/ml/tests/fixtures/*.onnx
!ml/registry/**/*.onnx

# Editors / OS
.DS_Store
.idea/
.vscode/
*.egg-info/
