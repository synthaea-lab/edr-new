//! # watchdog
//!
//! Watchdog binary. Monitors the agent process, restarts it on crash or kill, verifies
//! binary integrity, and reports tamper attempts. Agent silence is itself a detection.
//!
//! To be migrated from `old/watchdog`.

fn main() {
    // Intentionally empty — skeleton only.
}
